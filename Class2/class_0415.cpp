#include<iostream>
#include "MyFirst.h"
#include "MyFunction.h"


using namespace std;

/*
int Sum(int n);
int RecursiveSum(int n);
void local();
//void global();

//int j 500;
static int j = 500;

int global_A = 5; //전역변수

static int static_A;
*/

/*
int main()
{
	//cout << Sum(10) << endl;
	//cout << RecursiveSum(10) << endl;

	
	cout << "전역변수 j : " << ::j << endl;

	int i = 5;
	int j = 10;

	in global_A = 5; //전역변수

	int main()

		//	cout << "main()지역변수 i : " << i << endl;
		//	cout << "main()지역변수 j : " << j << endl;
		//	cout << "전역변수 j : " <<:: j << endl;
		//	local();
		//	cout << "전역변수 j : " <<:: j << endl;
		


		//F::PrinVariable(local_A);

	//	int local_A = 0; //지역변수
	//cout << global_A << endl;
	//cout << local_A << endl;

	
	cout << "외부 정적 변수" << static_A << endl;
	Checklncrement();
	Checklncrement();
	Checklncrement();

	static_A++;
	cout << "외부 정적 변수" << static_A << endl;
	

	
	enum ClassType
	{
		CPP =5,
		UNREAL = -1,
		JAVA = 3,
		C_3 = 8
}; 

	
	//ClassType MyClass = CPP;
	//ClassType MyClass = UNREAL;
	ClassType MyClass(CPP) ;
	ClassType MyUnreal(UNREAL);
	ClassType MyJava(JAVA);
	ClassType MyC_3(C_3);

	struct info
	{
		int Age;
		string name;
		float height;
	};
	
	cout << MyClass << endl;
	

	//F::PrintVariable(local_A);

	//cout << local_A << endl;
	//cout << global_A << endl;
}
int Sum(int n)

{
	int result = 0;
	for (int i = 1; i <= n; i++)
	{
				result += i;
	}
	return 0;
}

int RecursiveSum(int n)
{
	if (n == 1)
	{
		return 1;
	}
	cout << "n 값 : " << n << endl;
return n + RecursiveSum(n - 1);
	//1RecursiveSum(10) + RecursiveSum(9)+ RecursiveSum(8)... + RecursiveSum(1)
	//10 + 9 + 8 ... + 1
	
	
}
void local()
{
	int j = 20;
	cout << "local() 지역변수 j : " << j << endl;
}*/




